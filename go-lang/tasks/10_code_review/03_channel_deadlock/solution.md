# Решение

## Разбор

1. Найти все проблемы в коде (deadlock, недостижимый код, лишняя синхронизация,
   игнорирование переданного в горутину параметра `group`, отсутствие закрытия канала,
   бессмысленный `select` с одним `case`, именование).
2. Объяснить, почему программа зависает.
3. Переписать код правильно: закрывать канал после `wg.Wait()` в отдельной горутине
   и читать из канала через `for range`.

Полный исправленный вариант: [`fixed.go`](fixed.go) (запускается: `go run fixed.go`).
