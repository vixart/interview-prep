# Решение

## Подход

ожидается решение двумя указателями за `O(k)` — расширять окно
влево/вправо от `index`, на каждом шаге выбирая более близкий элемент,
а при равенстве расстояний — больший (то есть правый).
