# Решение

## Подход

ожидается решение за `O(n + m)` — построить множество
(`map[string]struct{}`) из уже существующих значений, а не сравнивать
каждый новый элемент со всеми старыми.

Рабочий код: [`solution.go`](solution.go) (запускается: `go run solution.go`).
