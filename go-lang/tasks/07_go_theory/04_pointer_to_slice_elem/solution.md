# Решение

## Разбор

1. Предсказать вывод.
2. Объяснить: у слайса из 3 элементов `len == cap == 3`, поэтому `append` выделяет
   **новый** массив и копирует данные. `carPtr` продолжает указывать на элемент
   **старого** массива, и второе `+= 50` уже не влияет на `cars[0]`.
3. Обсудить, почему хранить указатели на элементы слайса опасно.

## Ожидаемый вывод

```
5100 red
5150 red
```
