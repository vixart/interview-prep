# 4.3.4. Генератор слайса из N уникальных случайных чисел

Тип задачи: задача на написание кода вживую.

## Условие

Написать функцию `uniqN(n int) []int`, которая возвращает слайс из `n`
**уникальных** случайных чисел.

```go
package main

import (
    "fmt"
    "math/rand"
)

func main() {
    fmt.Println(uniqN(10))
}

func uniqN(n int) []int {
    // ...
}
```

**Задание:** реализуй функцию `uniqN`.
