# 4.6.11. Параллельный поиск на нескольких серверах

Раздел: `concurrency / 11_search_in_many_services`

## Условие

Реализовать функцию `Search` для параллельного поиска на нескольких серверах.

### Требования

- Принимает список серверов и поисковый запрос.
- Выполняет параллельный поиск на всех серверах.
- Возвращает первый успешный результат.
- Если все серверы вернули ошибку, возвращает ошибку.

## Заготовка

```go
package main

// SearchFunc - функция поиска на одном сервере (будет предоставлена).
type SearchFunc func(server, query string) ([]string, error)

// Search выполняет параллельный поиск на нескольких серверах.
func Search(servers []string, query string, searchFunc SearchFunc) ([]string, error) {
    // TODO: реализуйте параллельный поиск
    panic("implement me")
}
```
