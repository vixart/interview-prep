// Тестируемый код. Тест лежит рядом, в adder_test.go того же пакета,
// поэтому ему доступны и неэкспортируемые функции (addNumbers).
package adder

func addNumbers(x, y int) int {
	// функция неэкспортируемая, но тест лежит в том же пакете и видит ее
	return x + y // в книге здесь намеренный баг: x + x
}
